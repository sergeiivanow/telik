import 'package:flutter/material.dart';

class ChannelsListItemWidget extends StatelessWidget {
  const ChannelsListItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 300,
      child: Container(
        width: 200.0,
        color: Colors.red,
      ),
    );
  }
}
